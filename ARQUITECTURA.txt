╔═════════════════════════════════════════════════════════════════════════╗
║                    ARQUITECTURA DEL PROYECTO GESTORX                     ║
╚═════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│ LOCALHOST (XAMPP)                                                       │
└─────────────────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────────────┐
    │ http://localhost/htdocs                                    │
    └────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
        ┌───────▼──────────┐   ┌──────▼──────────┐
        │  GestorX/        │   │ (otros sitios)  │
        │  gestorx/        │   │                 │
        │  (Frontend)      │   │                 │
        └───────┬──────────┘   └─────────────────┘
                │
    ┌───────────┴──────────────────────────┐
    │                                      │
    │ npm run serve (puerto 8081)          │
    │ http://localhost:8081                │
    │                                      │
    └───────────────┬──────────────────────┘
                    │
        ┌───────────┘
        │
        ▼ (archivo index.html)
    ┌────────────────────────────┐
    │  Vue.js App               │
    │  - App.vue                │
    │  - main.js                │
    │  - router/index.js        │
    │  - views/                 │
    │    • Login.vue            │
    │    • Register.vue         │
    │    • Dashboard.vue        │
    │    • Usuarios/            │
    └────────────────┬───────────┘
                     │
                     │ (Solicitudes HTTP)
                     │ API_URL = '/gestorx-backend/api'
                     │
                     ▼
    ┌──────────────────────────────────────┐
    │  SOLICITUD HTTP RELATIVA             │
    │  /gestorx-backend/api/auth.php       │
    │  /gestorx-backend/api/usuarios.php   │
    │  /gestorx-backend/api/roles.php      │
    │  /gestorx-backend/api/registro.php   │
    └──────────────────┬───────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼ Se resuelve como:          ▼
    http://localhost/gestorx-backend/api/auth.php
                       │
                       ▼
    ┌────────────────────────────────────────┐
    │ XAMPP - Apache                         │
    │ Sirve archivos desde /htdocs           │
    └────────────────┬───────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────┐
    │  GestorX/                              │
    │  ├── gestorx/ (Frontend Vue.js)        │
    │  │   ├── src/                          │
    │  │   ├── public/                       │
    │  │   └── ...                           │
    │  │                                     │
    │  └── gestorx-backend/ (Backend PHP) ◄──┴─ RUTA ENCONTRADA
    │      ├── api/                          │
    │      │   ├── auth.php ◄────────────────┴─ ARCHIVO DEVUELTO
    │      │   ├── usuarios.php              │
    │      │   ├── roles.php                 │
    │      │   └── registro.php              │
    │      ├── config/                       │
    │      │   └── database.php              │
    │      └── ...                           │
    │                                        │
    └────────────────────────────────────────┘

╔═════════════════════════════════════════════════════════════════════════╗
║                          FLUJO DE AUTENTICACIÓN                         ║
╚═════════════════════════════════════════════════════════════════════════╝

Usuario entra a: http://localhost:8081/login

┌─────────────────────────────────────┐
│ 1. Usuario ingresa credenciales     │
│    - Correo                         │
│    - Contraseña                     │
└─────────────────────┬───────────────┘
                      │
                      ▼
        ┌─────────────────────────────┐
        │ 2. Click en "Iniciar Sesión"│
        └─────────────────┬───────────┘
                          │
                          ▼
        ┌──────────────────────────────────────┐
        │ 3. axios.post()                      │
        │   POST /gestorx-backend/api/auth.php │
        │   {                                  │
        │     action: 'login',                 │
        │     correo: '...',                   │
        │     password: '...'                  │
        │   }                                  │
        └─────────────────┬────────────────────┘
                          │
                          ▼ (Resuelto a http://localhost/gestorx-backend/api/auth.php)
        ┌──────────────────────────────────────┐
        │ 4. Backend procesa en PHP            │
        │    - Valida credenciales             │
        │    - Genera JWT token                │
        │    - Retorna usuario y token         │
        └─────────────────┬────────────────────┘
                          │
                          ▼
        ┌──────────────────────────────────────┐
        │ 5. Respuesta JSON                    │
        │   {                                  │
        │     success: true,                   │
        │     token: 'eyJhbGc...',             │
        │     user: { id, nombre, rol }       │
        │   }                                  │
        └─────────────────┬────────────────────┘
                          │
                          ▼
        ┌──────────────────────────────────────┐
        │ 6. Frontend (Login.vue)              │
        │    - Guarda token en localStorage    │
        │    - Guarda usuario en localStorage  │
        │    - Redirige a /dashboard           │
        └──────────────────────────────────────┘

╔═════════════════════════════════════════════════════════════════════════╗
║                        ESTRUCTURA DE CARPETAS FINAL                     ║
╚═════════════════════════════════════════════════════════════════════════╝

GestorX/
│
├── gestorx/                          ← FRONTEND VUE.JS (puerto 8081)
│   ├── public/
│   │   └── index.html               (Punto de entrada)
│   │
│   ├── src/
│   │   ├── App.vue                  (Componente raíz)
│   │   ├── main.js                  (Inicialización)
│   │   │
│   │   ├── config/
│   │   │   └── api.js               ⭐ CONFIGURACIÓN DE URLs
│   │   │
│   │   ├── router/
│   │   │   └── index.js             (Rutas de la app)
│   │   │
│   │   └── views/
│   │       ├── Dashboard.vue
│   │       ├── Login.vue            (✅ USA /gestorx-backend/api)
│   │       ├── Register.vue         (✅ USA /gestorx-backend/api)
│   │       │
│   │       ├── Layout/
│   │       │   ├── AdminLayout.vue
│   │       │   └── UserLayout.vue
│   │       │
│   │       └── Usuarios/
│   │           ├── ListaUsuarios.vue    (✅ USA /gestorx-backend/api)
│   │           ├── CrearUsuario.vue     (✅ USA /gestorx-backend/api)
│   │           └── EditarUsuario.vue    (✅ USA /gestorx-backend/api)
│   │
│   ├── package.json
│   ├── babel.config.js
│   ├── vue.config.js
│   ├── jsconfig.json
│   │
│   ├── INTEGRACION_BACKEND.md        (📖 Documentación)
│   └── CAMBIOS_REALIZADOS.txt        (📋 Checklist)
│
└── gestorx-backend/                  ← BACKEND PHP (http://localhost)
    ├── api/
    │   ├── auth.php                 (Login/Register)
    │   ├── usuarios.php             (CRUD usuarios)
    │   ├── roles.php                (Lista de roles)
    │   └── registro.php             (Registro de empresa)
    │
    ├── config/
    │   └── database.php             (Conexión DB)
    │
    ├── includes/
    │   └── (Funciones comunes)
    │
    └── ... (otros archivos)

╔═════════════════════════════════════════════════════════════════════════╗
║                          CÓMO ESTÁ CONFIGURADO                          ║
╚═════════════════════════════════════════════════════════════════════════╝

ANTES ❌:
  API_URL = 'http://localhost/gestorx-backend/api'
  → Problema: URL absoluta, solo funciona en localhost

DESPUÉS ✅:
  API_URL = '/gestorx-backend/api'
  → Ventaja: URL relativa, funciona en cualquier dominio/puerto

EJEMPLOS DE FUNCIONAMIENTO:

  1. En desarrollo (puerto 8081):
     http://localhost:8081
     → Solicita: /gestorx-backend/api/auth.php
     → Resuelve: http://localhost/gestorx-backend/api/auth.php ✓

  2. En producción (dominio):
     https://tu-empresa.com
     → Solicita: /gestorx-backend/api/auth.php
     → Resuelve: https://tu-empresa.com/gestorx-backend/api/auth.php ✓

  3. En servidor diferente:
     https://app.tu-empresa.com
     → Solicita: /gestorx-backend/api/auth.php
     → Resuelve: https://app.tu-empresa.com/gestorx-backend/api/auth.php ✓

╔═════════════════════════════════════════════════════════════════════════╗
║                            STATUS ACTUAL                                ║
╚═════════════════════════════════════════════════════════════════════════╝

✅ Frontend: Listo (npm run serve en puerto 8081)
✅ Backend: Integrado (en carpeta gestorx-backend)
✅ URLs: Actualizadas a rutas relativas
✅ Configuración: Centralizada (config/api.js)
✅ Documentación: Completa

🚀 ¡LISTO PARA USAR!
